#ifndef DISPLAY_CONFIG_H
#define DISPLAY_CONFIG_H

// Delay (ms) between switching digits in multiplexing
const int DIGIT_REFRESH_DELAY_MS = 1;

// Optionally show decimal point
const bool USE_DECIMAL_POINT = true;

#endif